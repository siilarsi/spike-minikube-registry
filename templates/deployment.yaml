apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: registry
  name: registry
  namespace: spike
spec:
  replicas: 1
  selector:
    matchLabels:
      app: registry
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: registry
    spec:
      containers:
      - image: registry:2
        name: registry
        volumeMounts:
        - name: repository-volume
          mountPath: "/var/lib/registry"
        resources: {}
      volumes:
      - name: repository-volume
        persistentVolumeClaim:
          claimName: registry

status: {}
